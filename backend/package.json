{
  "name": "claude-free-server",
  "version": "1.1.0",
  "description": "Autonomes, selbstheilendes Agenten-System",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node-dev --respawn index.ts",
    "type-check": "tsc --noEmit",
    "install-deps": "npm install"
  },
  "dependencies": {
    "@hono/node-server": "^1.6.0",
    "hono": "^4.0.0",
    "socket.io": "^4.7.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0",
    "ts-node-dev": "^2.0.0"
  }
}
